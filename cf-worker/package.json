{
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "deploy": "npx wrangler publish",
    "dev": "npx wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^2.12.1"
  },
  "dependencies": {
    "@cloudflare/kv-asset-handler": "^0.3.0"
  }
}
